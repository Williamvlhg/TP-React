

const Header = ({ children }) => {

    return (
        <header className="App-header">
            <div>
            {children}
            </div>
        </header>
    )


}

export default Header;